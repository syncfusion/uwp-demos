<Page
    x:Class="Common.WhatsNewContentPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Common"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <Grid  Background="{ThemeResource ApplicationPageBackgroundThemeBrush}" VerticalAlignment="Stretch" HorizontalAlignment="Stretch">
    <Grid.RowDefinitions>
        <RowDefinition Height="Auto"/>
        <RowDefinition Height="Auto"/>
        <RowDefinition Height="*"/>
        <RowDefinition/>
    </Grid.RowDefinitions>
    <TextBlock Text="{Binding Header}" FontSize="15" Margin="5"/>
    <TextBlock Text="{Binding Description}" Grid.Row="1" FontSize="9" Margin="5"/>
    <Grid Grid.Row="2" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" x:Name="viewGridContent" Margin="5" />
    </Grid>
</Page>
